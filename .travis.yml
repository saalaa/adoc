language: python

python:
    - 3.5
    - 3.6

install:
    - pip install -r requirements-test.txt
    - pip install -r requirements.txt

jobs:
  include:
    - stage: analysis
      script: bandit -r adoc
    - stage: test
      script: pytest .
